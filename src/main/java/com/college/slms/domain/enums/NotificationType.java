package com.college.slms.domain.enums;

/**
 * Categories of student-facing notifications required by the specification.
 */
public enum NotificationType {
    REQUEST_APPROVED,
    REQUEST_REJECTED,
    ISSUE,
    RETURN,
    DUE_REMINDER,
    FINE,
    GENERAL
}
